#include<stdio.h>
struct _fraction
{
int num,den;
};
typedef struct _fraction fraction;
fraction input_fraction()
{
  fraction f;
  printf("Enter fraction");
  scanf("%d %d",&f.num,&f.den);
  return f;
}
faction largest_fraction(fraction f1,fration f2,fraction f3)
{
  int sden=f1.den*f2.den*f3.den
  

  
}